<li>
	{{burger_name}}

	{{#if devoured}}{{else}}<button class="devour-burger" data-id="{{id}}" data-eatBurger="{{devoured}}">
		Chow Down!
	</button>{{/if}}

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
